namespace Domain.Enums
{
    // How engaged the student was during the session.
    public enum ParticipationRating
    {
        Active = 1,   // متفاعل
        Moderate = 2, // متوسط
        Quiet = 3     // صامت
    }
}
