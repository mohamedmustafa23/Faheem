namespace Domain.Enums
{
    // Overall performance the teacher taps for a student after a session.
    public enum PerformanceRating
    {
        Excellent = 1, // ممتاز
        Good = 2,      // كويس
        Average = 3,   // متوسط
        NeedsWork = 4  // محتاج مجهود
    }
}
